# Tokens test plan

## Synchronization

- Add an account by searching for a token
- Migrate an account : Add an account in prod, don’t crash using tested version
- Synchronizing manually does not throw an error (green check)
- Synchronizing of a big account (which uses multiple pages when fetching history)

## Receive/Address verification

- User can try to receive a token and address is right

## Balance

- Available balance of each token is right

## Broadcast

- Send max tokens empties the token account
- Send token operation sends the right amount
- User cannot send more than his token balance

## Operations

- Optimistic operation is filled correctly
- Operation history has every transactions of the token account
- Operation has the right transaction ID
- User can reach the tx details by clicking on the “view on explorer” button
- Operation token amount is right
- Fees shown are right

## Account details

- Fiat value fetched from countervalues is right
